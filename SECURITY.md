# Security reporting

Do not publish secrets, game binaries, private paths, or exploit-bearing assets.
A private reporting channel is not configured yet. Share only a sanitized problem
description until one exists. Parsers must reject invalid bounds and report errors;
never convert failed parsing into a compatibility success.
